Activate the prototype-generator skill located at `.agents/skills/prototype-generator/SKILL.md`.

This skill generates a runnable React prototype from a screen spec (output of spec-analyzer). It produces interactive artifacts with navigation, fake data, modals, and UI interactions.

Read the SKILL.md file first, then follow its pipeline exactly.
