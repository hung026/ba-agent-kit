Activate the ui-descriptor skill located at `.agents/skills/ui-descriptor/SKILL.md`.

This skill describes a UI from a screenshot or Figma link into a detailed component table — component name, type, state, description, validation, and API mapping.

Read the SKILL.md file first, then follow its pipeline exactly.
