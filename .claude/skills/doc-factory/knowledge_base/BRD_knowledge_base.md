When writing functional specifications, always define/consider:
- **Default State**: Initial UI state (e.g., Empty state: "No history. Create new conversation here").
- **Sorting/Ordering Rules**: Default data organization logic (e.g., "Sort by latest created date").
